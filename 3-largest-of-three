public static Integer findLargest(Integer num1, Integer num2, Integer num3) {
      // Use Math.max to find the largest between the three integers
        Integer maxOfFirstTwo = Math.max(num1, num2);
        return Math.max(maxOfFirstTwo, num3);
}
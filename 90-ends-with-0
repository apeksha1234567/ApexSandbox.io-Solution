public Boolean isEndWithZero(Integer num){
    // Calculate the remainder using Math.mod() and check with if/else statement
        if (Math.mod(number, 10) == 0) {
            return true;  // Number ends with 0
        } else {
            return false; // Number does not end with 0
        }
}
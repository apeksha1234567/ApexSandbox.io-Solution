public String evenOrOdd(Integer num) {
    // Compute the remainder after dividing by 2 using Math.mod
        Integer remainder = Math.mod(number, 2);
        
        // If the remainder is 0, it's even; otherwise, it's odd
        if (remainder == 0) {
            return 'even';
        } else {
            return 'odd';
        }
    }
}
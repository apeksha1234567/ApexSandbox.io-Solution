public Decimal computeTip(Decimal total, Integer percent) {
     // Convert percentage to Decimal to ensure correct division (to avoid truncation)
        Decimal decimalPercentage = Decimal.valueOf(percentage);
        
        // Calculate the tip by multiplying the total bill by the percentage (as a Decimal) and dividing by 100
        Decimal tip = totalBill * decimalPercentage / 100;
        
        return tip;
    }
    
}